{
  "name": "jgan",
  "version": "1.0.0",
  "main": "dist/index.js",
  "module": "dist/jgan.esm.js",
  "source": "src/jgan.ts",
  "types": "dist/jgan.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "b": "tsdx build",
    "r": "rimraf dist/ && tsdx build --name jgan --format esm,cjs,umd",
    "prepublishOnly": "npm run build",
    "lint": "eslint src/**/*.{js,ts,tsx} && prettier --check src/**/*.{js,ts,tsx}",
    "format": "prettier --write src/**/*.{js,ts,tsx}",
    "test": "jest",
    "test-watch": "jest --watch",
    "test-cov": "npm test -- --coverage"
  },
  "devDependencies": {
    "@babel/core": "^7.12.17",
    "@babel/node": "^7.12.17",
    "@types/jest": "^26.0.20",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.1",
    "tsdx": "^0.14.1",
    "typescript": "^3.9.9"
  }
}
